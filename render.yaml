services:
  - type: web
    name: quantix
    env: node
    buildCommand: npm install && npm run build
    startCommand: cd server && npm install && node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        value: quantix-secret-2025
